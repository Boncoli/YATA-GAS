- [x] 2026-03-16: YATA.js本家からv1.2.0（ログの可観測性向上、設定のAppConfig集約、リベンジロジック追加）を同期し、ローカル環境(gas-bridge.js)の対応とドキュメント最新化を完了。
- [x] 2026-03-15: 昨晩の改修（XmlService.parseを空オブジェクトで返す変更）により正規表現フォールバックが発動せずクラッシュする問題を修正。
- [x] 2026-03-11: v1.0.1 アップデート（YATA.js本家修正の取り込み）および「Local版 All Test」の導入。
- [x] 2026-03-11: 要約失敗時のベクトルズレ修正、GeminiのJSON強制条件化、パーサーの型安全化を完了。
- [x] 2026-03-11: 機器に優しいインメモリDB (:memory:) を用いた全ロジック検証用スクリプト `tests/test-local-all.js` を実装。
- [x] 2026-03-10: v1.1.2 バグ修正（コンテキスト圧縮の無駄打ち防止）とデイリートレンド実装を完了し、OSSへ反映。
- [x] 2026-03-10: YATA.js (main) 最新化と prompt_company.json の統合完了。JSONモードとバッジレンダリング導入。
# YATA Project TODO & Ambitions

YATA (boncoli RasPi) をさらに進化させ、真の「パーソナル・AI・パートナー」へと昇華させるためのロードマップ。

## 📊 次なるライフログ・パーソナライズ (Next Evolution)
- [ ] **デジタル・ツイン (分身) の構築と深化**:
    - 「独り言キャッチャー」で得た生の思考プロセスを Gemini で分析し、BON 様の価値観や判断基準をプロファイリング。
    - ニュースや事象に対し「BON 様ならどう考え、どう反応するか」をシミュレート・代弁できるレベルまで精度を上げる。
- [ ] **興味関心ベースの「意味的マッチング」向上**:
    - 現在のキーワード＋AI要約ピックアップをさらに強化。ベクトル検索を使い、キーワードが直接一致しなくても「文脈的に近い」ニュースを確実に捉える。
- [ ] **Apple Watch 連携の安定化**:
    - 外出先からの Watch 単体送信の成功率向上（リトライ処理の実装）。
- [x] **ローカル LLM (Gemma 3) の実用化**:
    - [x] **ローカル独り言の自律運用**: `tasks/do-local-mutter.py` を実装し、1時間おき（時報）の自律的な「ボヤキ」を開始。Gemma 3 4Bによる高度なデジタルツイン発話を実現。
    - [x] **爆速ローカル対話の実現 (2026/03/09)**: `llama-cpp-python server` による常駐化（yata-ai-server）を達成。ロード時間をゼロにし、Discordでの高速レスポンスを実現。
    - [x] **状況把握能力の統合**: 会話時に「位置情報」「天気」「CPU温度」をリアルタイムで読み込み、文脈に応じた返答を行うロジックを Discord ボットへ実装。
    - [x] **オフライン・バックアップ**: ネットワーク遮断時や熱暴走時（75℃以上）に自動でクラウド LLM (OpenAI/Gemini) へ切り替えるフェイルセーフを実装。
- [ ] **ローカル音声合成 (TTS)**:
    - 朝のDiscord要約を、ラズパイが自室や車内で読み上げ。

## 🗺️ 地図の進化 (Map Evolution)
- [ ] **タイムライン・フィルタリングの高度化**:
    - 指定した期間の軌跡のみを表示し、その期間の総走行距離や平均燃費も併記する。
- [ ] **ヒートマップ表示**:
    - よく通る道（CX-80の活動圏内）を視覚的に強調。

- [x] **YATA.js 2nd Vectorization (Method Embedding) の実装と 256次元圧縮による極限最適化 (2026/03/04)**:
    - 記事の「話題 (Topic)」に加え、「測定手法・原理 (Method)」に特化した第2ベクトルを生成・保存する機能を実装。
    - 異分野間で同じ手法が使われ始めた「技術パラダイムの兆候」を検知できる高度な分析基盤（R&D特化）を確立。
    - `text-embedding-3-small` のネイティブ機能（Matryoshka Representation Learning）を利用し、ベクトル次元を1536→256次元へ圧縮。精度劣化を数%に抑えつつGASのデータ容量を1/6に激減させた。
- [x] **ヘルスケアデータ同期の信頼性向上 (2026/03/03)**:
    - iPhoneショートカットからのデータ受信時に発生していたSQLパラメータ不足エラーを、名前付きパラメータ（`:date`, `:type`等）の導入により解消。
- [x] **デジタルツインの高度な統合とローカルLLM（Gemma 3 4B）の自律運用開始 (2026/03/03)**:
    - 複数モデル（GPT-5, 27B等）の解析結果を `synthesized_master_persona.md` に集約。ボヤキ生成のコアエンジンとして搭載。
    - 3コア制限（n_threads=3）をかけた `tasks/do-local-mutter.py` を実装。システム負荷を抑えつつ、直近24時間のニュースと動静に基づいた「自然なボヤキ」を1時間おきにDiscordへ放流開始。
- [x] **DB軽量化と長期運用体制の確立 (2026/03/03)**:
    - `tasks/archive-old-articles.js` を動的日付計算（180日）に修正。半年サイクルでの自動アーカイブ・VACUUMを可能にした。
- [x] **ダッシュボードの視覚的品質改善 (2026/03/03)**:
    - フォールバック文字変換（normalize_text）を `server.js` に実装。全角記号による「豆腐化（□）」を根絶。
### 2026/03/08: ドキュメントの3層構造化、OSS公開、および正規レポート配信のローカル化
- [x] **ドキュメントの3層構造化とOSS公開準備の完遂**:
    - ドキュメントを `README` (GAS本家), `README_LOCAL` (ポータル), `PROJECT_GUIDE` (技術仕様) に再編。
    - GitHub 公開用リポジトリ `YATA-GAS` を新規作成し、クリーンな「初版」を公開。
- [x] **正規レポート配信のローカル化と安全性強化**:
    - **本家 YATA.js の完全保護**: `lib/YATA.js` を 1 文字も汚さず、すべてを `gas-bridge.js` で吸収する設計を死守。
    - **DRY_RUN 機能の実装**: `.env` に `DRY_RUN="TRUE"` を書くだけで、本家コード側の `PropertiesService` や `History` への書き込みをスキップする安全装置を bridge 側に搭載。
    - **HTML プロンプト化**: Markdown 出力によるデザイン崩れを防ぐため、`prompts.json` のテンプレートを HTML 指定に変更。
    - [x] **テストタスクの整備**: 本家の `dailyDigestJob` および `sendPersonalizedReport` をローカルから実行できるスクリプト (`tasks/do-run-daily.js`, `tasks/do-run-trend.js` 等) を作成。
    - **セマンティック検索の動作確認**: ローカルのベクトル検索を用いた正規レポート配信の成功を確認。

- [x] **システムの堅牢化と時限爆弾の解体 (2026/03/06)**:
    - **OOMとOSフリーズの解消**: `gas-bridge.js` の同期通信を `sync-fetch` からLinux標準の `curl` に置き換え、メモリ枯渇を根本的に解決。
    - **ログの3層構造化**: 揮発性ログ（RAM）、長期保存（NAS）、致命的エラー（SD）を明確に分離し、SDカードの書き込みをゼロ化しつつ証拠保全を両立。
    - **熱暴走とコアダンプ防止**: ローカルLLM実行前にCPU温度（70度）チェックを導入。また、独り言処理の `fetch` を `https` に置換しゴミデータの大量発生を防止。
- [x] **リポジトリの整理 (2026/03/03)**:
    - 大容量JSON生データをGit追跡から除外、動的な `interests.json` も追跡除外。運用ドキュメントに「プロセス・マップ」を追記し、全体像の視認性を向上。
- [x] **YATA.js APIコスト浪費の無限ループ修正とプロンプト厳格化 (2026/03/02)**:
    - 2月の月またぎ（28日問題）に起因する、ベクトル削除（1ヶ月前）と生成（30日前）の閾値競合による「無限再生成ループ」を解消。削除を35日前に固定。
    - APIエラー時の無限リトライ防止ロジックと、最新の厳格・客観的事実抽出型プロンプトを導入。
    - `gpt-5-nano` の推論量を `minimal` から `low` に微調整。
- [x] **AI独り言の情緒強化と個人情報マスク処理 (2026/03/02)**:
    - 発話リズムを時計の5分刻みに同期させ、より自然な存在感を演出。
    - 外部APIへのプロファイル送信時に特定個人情報（地域・会社）を自動マスクするセキュリティ機能を実装。
- [x] **ローカルLLM (Gemma 3 4B) による長時間バックグラウンド分析 (2026/03/01)**:
    - 18万件のアーカイブデータを100件ずつのチャンクに分割し、状態を記録しながら一晩かけて処理する「ダラダラ分析スクリプト」を実装。
    - 熱暴走対策のクールダウン機能とレジューム機能を搭載し、ローカルでの持続的な深層プロファイリング環境を確立。
### [実績] 2026年3月
- [x] **潜在的な時限爆弾の解体と予防措置の徹底 (2026/03/06)**:
    - `pm2-logrotate` を導入し、PM2の無限ログ蓄積によるSDカード寿命低下を防止。
    - ローカルLLM (`do-local-mutter.py`) 実行前にCPU温度をチェックし、70℃以上なら起動をスキップする熱暴走・フリーズ防止のフェイルセーフを実装。
    - `process-mutter.js` の終了時クラッシュ（`core` ダンプ生成）を `https` モジュールへの書き換えにより根絶。
    - Cronの重いログ出力をRAMディスクへ逃がし、バックアップ時のRAM完全お掃除ロジックを確立。
- [x] **OOM枯渇とOSフリーズ問題の完全解消 (2026/03/06)**:
    - `gas-bridge.js` における同期通信エミュレートのために使用していた `sync-fetch` が、裏で大量のNodeワーカープロセスを起動しメモリを食いつぶしていた問題を特定。
    - `sync-fetch` を完全撤去し、Linux標準の `curl` コマンドを用いた超軽量な同期通信に書き換え。プロセス乱造とメモリ肥大化を根絶し、安定した大量RSS処理を実現した。
- [x] **ヘルスケア統合エンジン (daily_health) の安定化 & YATA.js 最新化 (2026/03/05)**:
    - `better-sqlite3` のパラメータパースエラーを修正。iOS ロック解除時の手動/自動運用を推奨。
    - **YATA.js 最新同期**: `Method Embedding (2nd Vectorization)` および `256次元圧縮 (Native Dimensionality Reduction)` を導入。
    - 直近7日間（324件）の過去記事に対し、Method Vector のバックフィル（一括付与）を完了。
    - 予兆検知エンジンの判定軸を「トピック」から「手法・原理」へと拡張可能にした。
- [x] **DBクリーンアップ: health_logs テーブルの削除 (2026/03/01)**:
    - 2/27の統合エンジン移行後の残骸を削除し、`daily_health` へ一本化。
- [x] **完全版・高濃度ライフログ・アーカイブ (41.24MB) の構築 (2026/02/28)**:
    - X (18万件) と YouTube (3.8万件) の全生データを統合・構造最適化。
- [x] **X (Twitter) データ統合と GPT-5-mini による究極のデジタルツイン解析 (2026/02/28)**:
    - OpenAI の最新推論モデル `gpt-5-mini` を用い、32,000トークンの深層解析を実施。
- [x] **完全ローカル・大規模言語モデル (Gemma 3) の実装 (2026/02/28)**:
    - Raspberry Pi 5 (8GB) 上に `Gemma 3 1B IT` を導入し、秒間 9 tokens の推論環境を確立。
- [x] **完全ローカル・独り言キャッチャーの実装 (2026/02/28)**:
    - ラズパイ5と `faster-whisper` を用いて、RAMディスク完結の音声自動文字起こしデーモンを常駐化。
- [x] **ダイナミック・ペルソナ・エディタの実装 (2026/02/27)**:
    - ポータルサイト（`portal.html`）から `persona.txt` を直接編集・保存できるUIを構築。
- [x] **YouTubeデータ連携による究極のパーソナライズ実装 (2026/02/27)**:
    - Google Takeoutの視聴履歴を全件解析し、Discordダイジェストのスコアリングに統合。
- [x] **統合ライフログ・エンジン (daily_health) の拡張 (2026/02/27)**:
    - iPhone（ショートカット）から歩数、睡眠、HRV、安静時心拍、消費カロリーを1日1行に集約する仕組みを完遂。

---
*「一歩ずつ、理想のライフログへ。🚗💨」*
